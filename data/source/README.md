# source

CSV files for maintaining databases of things. The corresponding `*.lua` files should not be updated manually, but instead handled in the .csv files.

Run `generate-all-csv.bat` or `generate-and-deploy.bat` to update lua files from csv.

